Config = {
    --- Phone script (yseries, lb, npwd, qb, qs any others please request or make a PR)
    PhoneScript = "lb",
    --- Inventory script (ox, qb and qs  any others please request or make a PR)
    InventoryScript = "ox",
    --- Framework (qb, qbx or esx - sandbox framework support to come soon.)
    Framework = "ox",
    --- Notifications (qb, esx, ox or custom - Check the notifications folder for more info)
    Notifications = "ox",
    --- Group system (yseries, ps, slrn, custom, kmack_lib(soon))
    GroupSystem = "slrn",
    --- Logs (ox, discord, custom)
    Logs = "ox",
}

return Config